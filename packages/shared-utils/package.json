{
  "name": "@career-os/shared-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Zero-domain utility functions shared across all Career OS packages and apps. No business logic, no domain types — pure utilities (P5).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "type-check": "tsc --noEmit --project tsconfig.json",
    "lint": "eslint src/",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
