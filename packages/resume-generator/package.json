{
  "name": "@career-os/resume-generator",
  "version": "0.0.0",
  "private": true,
  "description": "Transforms ContentGraph into resume artifacts (LaTeX source, PDF). Implements the Generator<ResumeConfig> interface. Writes to output/resume/ — never to content/raw/ (P1, P6).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "type-check": "tsc --noEmit --project tsconfig.json",
    "lint": "eslint src/",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@career-os/content-schema": "workspace:*",
    "@career-os/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
