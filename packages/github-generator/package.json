{
  "name": "@career-os/github-generator",
  "version": "0.0.0",
  "private": true,
  "description": "Transforms ContentGraph into GitHub profile README and project READMEs. Implements Generator<GitHubConfig>. Writes to output/github-profile/ — never to content/raw/ (P1, P6).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "type-check": "tsc --noEmit --project tsconfig.json",
    "lint": "eslint src/",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@career-os/content-schema": "workspace:*",
    "@career-os/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
